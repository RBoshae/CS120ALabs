


// Instantiate the module
flight_attendant_call_system_bm instance_name (
    .clk(clk), 
    .call_button(call_button), 
    .cancel_button(cancel_button), 
    .light_state(light_state)
    );


